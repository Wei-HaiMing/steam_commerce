<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Quote</title>
</head>
<body>
    <%- include('partials/nav') %>

    <h1>Edit Quote</h1>

    <form method="POST" action="/editQuotes">
        <input type="hidden" name="quoteId" value="<%= QuoteInfo[0].quoteId %>">

        <label>Quote:<br>
            <textarea name="quote" rows="4" cols="50"><%= QuoteInfo[0].quote %></textarea>
        </label>
        <br><br>

        <label>Category:
            <input name="category" type="text" value="<%= QuoteInfo[0].category %>">
        </label>
        <br><br>

        <label>Likes:
            <input name="likes" type="number" value="<%= QuoteInfo[0].likes %>">
        </label>
        <br><br>

        <button type="submit">Edit Quote</button>
    </form>

</body>
</html>
